runtime: nodejs12

env: standard

env_variables:
  JWT_SECRET: "<%- JWT_SECRET %>"
  EASYNC_API_TOKEN: "<%- EASYNC_API_TOKEN %>"

handlers:
- url: /api/.*
  secure: always
  script: auto

- url: /.*
  static_dir: static